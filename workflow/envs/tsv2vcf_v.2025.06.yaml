name: tsv2vcf_v.2025.06
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - biopython=1.85 # from Bio import SeqIO
  - numpy=2.3.0    # numpy.array
  - scipy=1.15.2   # from scipy.stats import fisher_exact

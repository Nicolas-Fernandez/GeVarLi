---

###I###R###D######U###2###3###3#######T###R###A###N###S###V###I###H###M###I####
###                                                                         ###
###    /\  ______      ___ ____ _  _ __   ____ __   ____     ______  /\     ###
###    ||  \ \ \ \    / __| ___| \/ )__\ (  _ (  ) (_  _)   / / / /  ||     ###
###    ||   > > > >  ( (_-.)__) \  /(__)\ )   /)(__ _)(_   < < < <   ||     ###
###    ||  /_/_/_/    \___(____) \(__)(__|_)\_|____|____)   \_\_\_\  ||     ###
###    \/                                                            \/     ###
###                                                                         ###
###I###R###D######U###2###3###3#######T###R###A###N###S###V###I###H###M###I####
# Name ___________________ config.yaml
# Version ________________ v.2023.06
# Author _________________ Nicolas Fernandez
# Affiliation ____________ IRD_U233_TransVIHMI
# Aim ____________________ Configuration yaml file for gevarli.smk snakefile
# Date ___________________ 2021.10.12
# Latest modifications ___ 2024.01.26 (add minimap2 aligner)
# Use ____________________ Edit default settings

###############################################################################
### RESOURCES ###
#################
resources:
  cpus: '8'          # Set maximum cpus usage (for multithreading, default: "8") [INT]
  ram: '12'          # Set maximum memory usage (in Gb, default: "16") [INT]
  tmp_dir: '$TMPDIR' # Temporary directory (default: "$TMPDIR")
  ### Examples:
    ### '$TMPDIR'  # System variable $TMPDIR (default)
    ### '.'        # Local (i.e. GeVarLi root directory)
    ### '/scratch' # HPC (set it to match your HPC usage)

###############################################################################
### CONSENSUS ###
#################
consensus:
  run: # Run consensus analyzes (default: yes)
  - 'yes' # Yes (default)
  #- 'no'  # No (run only quality control sub-workflow...)
  path: 'resources/genomes/' # Path to genomes references (default)
  reference: # Your reference, in fasta format (default: SARS-CoV-2_Wuhan_MN-908947-3)
  # Available options (not exhaustive), choose one:
    - 'SARS-CoV-2_Wuhan_MN-908947-3'        # SARS-CoV-2  (Nextclade and Pangolin)
    #- 'Monkeypox-virus_Zaire_AF-380138-1'   # Monkeypox   (Nextclade and Pangolin)
    #- 'Monkeypox-virus_UK_MT-903345-1'      # Monkeypox   (Nextclade and Pangolin)
    #- 'Swinepox-virus_India_MW-036632-1'    # Swinepox    (Nextclade)
    #- 'Ebola-virus_Zaire_AF-272001-1'       # Ebola       (na)
    #- 'Ebola-virus_Sudan_MH-121162-1'       # Ebola       (na)
    #- 'Lassa-virus_NC-004297-1_Seg-L'       # Lassa-L NC  (na)
    #- 'Lassa-virus_NC-004297-1_Seg-S'       # Lassa-S NC  (na)
    #- 'Lassa-virus_MT-193282-1_BEN-16131_Seg-L' # Lassa-L MT (na)
    #- 'Lassa-virus_MT-193282-1_BEN-16131_Seg-S' # Lassa-S MT (na)
    #- 'Marburg-virus_NC_001608-3'           # Marburg     (na)
    #- 'Dengue-virus-1_NC-001477-1'          # Dengue 1    (na)
    #- 'Dengue-virus-2_NC-001474-2'          # Dengue 2    (na)
    #- 'Dengue-virus-3_NC-001475-2'          # Dengue 3    (na)
    #- 'Dengue-virus-4_NC-002640-1'          # Dengue 4    (na)
    #- 'Nipah-virus_Malaysia_AJ-564622-1'    # Nipah       (na)
    #- 'HIV-1_HXB2_K-03455-1'                # HIV-1       (na)
    #- 'Human-herpesvirus-5_NC-006273-2'     # Herpes      (na)
    #- 'Human-herpesvirus-3_NC-001348-1'     # Varicella   (na)
    #- 'Human-adenovirus-A_NC-001460-1'      # Adeno       (na)
    #- 'Human-enterovirus-68_AY-426531-1'    # Entero      (na)
    #- 'Loa-loa_JH-712066-1_GCA_000183805-3' # Loa loa V3  (na)
    #- 'Add_your_favorite_genome_ref'        # Custom      (na)
  min_cov: # Minimum coverage, mask lower regions with "N" (default: "30") [INT]
  # Available options (not exhaustive), choose one:
    #- '60'                         # 60 x : for great coverage
    - '30'                         # 30 X : AFROSCREEN SARS-CoV-2 with Illumina amplicons sequencing (default)
    #- '10'                         # 10 X : for some applications...
    #- '3'                          #  3 X : only for exploration !
    #- 'Add_your_minimum_coverage'  # Custom minimum coverage 
  aligner: # Select your favorite aligner (default: 'bwa')
  # Available options, choose one:
    - 'minimap2' # A versatile sequence alignment program (default)
    #- 'bwa'      # Better, Faster, (Stronger, Harder...)
    #- 'bowtie2'  # Depreciated (slower),"sensitivity" requiried (see below "Bowtie2" options)

###############################################################################
### NEXTCLADE ###
#################
nextclade:
  run: # Run Nextclade analyzes (default: yes)
    - 'yes' # Yes (default)
    #- 'no' # No
  dataset: # Nextclade dataset
  # Available options (not exhaustive), choose one:
    - 'sars-cov-2' # SARS-CoV-2 (default)
    #- 'MPXV'       # MPox - all clades 
    #- 'hMPXV'      # human MPox
    #- 'hMPXV_B1'   # human MPox - only lineage B1*
  path: 'resources/nextclade/' # Path to nextclade datasets (default path)

###############################################################################
### PANGOLIN ###
################
pangolin:
  run: # Run Pangolin analyzes (default: no)
  #- 'yes' # Yes
  - 'no' # No (default)

###############################################################################
### MULTIQC ###
###############
multiqc:
  run: # Run Quality analyzes (default: yes)
  - 'yes' # Yes (default)
  #- 'no' # No
#  config: 'configuration/multiqc/default.yaml' # Path to the multiqc configuration file
#  tag: '' # Use only modules which tagged with this keyword

###############################################################################
### FASTQ-SCREEN ###
####################
fastq_screen:
  config: 'configuration/fastq-screen.conf' # Path to the fastq-screen configuration file
  subset: '10000' # Don't use whole sequences, but a subset reads (default: "10000") [INT] (0=all)

###############################################################################
### CUTADAPT ###
################
cutadapt:
  length: '50' # Discard reads shorter than 'length' after trim (default: "50")
  kits: # Sequence of an adapter ligated to the 3' end of the first read
    truseq:  'AGATCGGAAGAGC'         # Illumina "TruSeq" or "ScriptSeq" based libraries kits
    nextera: 'CTGTCTCTTATACACATC'    # Illumina "Nextera" or "TruSight" based libraries kits
    small:   'TGGAATTCTCGGGTGCCAAGG' # Illumina "Small" based libraries kits
  clipping: '0' # Cut 'n' first nucleotides reads, used for amplicon primers hard-clipping (default: 0)

###############################################################################
### SICKLE-TRIM ###
###################
sickle_trim:
  quality: '30'      # Phred score limit (default: "30")
  length: '50'       # Read length limit after trim (default: "50")
  command: 'pe'      # Reading (default and should be "pe" for paired-end sequences)
  encoding: 'sanger' # Quality encoding (default, and should be "sanger")
  # Only for information:
    #- 'sanger'   # for CASAVA >= 1.8 for "recent" Illumina reads (default)
    #- 'illumina' # for CASAVA 1.3 to 1.7 for "old" Illumina reads
    #- 'solexa'   # for CASAVA < 1.3 for Solexa

###############################################################################
### MINIMAP2 ###
################
minimap2:
  preset: # -x: Minimap2 presets (always applied before other options)
  # Available preset options, choose one: 
    - 'sr'        # genomic short-read mapping, like Illumina (default)
    #- 'map-ont'   # Nanopore vs reference mapping
    #- 'ava-ont'   # Nanopore read overlap
    #- 'splice'    # Long-read spliced alignment
    #- 'map-pb'    # PacBio vs reference mapping
    #- 'map-hifi'  # PacBio HiFi reads vs reference mapping
    #- 'ava-pb'    # PacBio read overlap
    #- 'splice:hq' # Pacbio-CCS spliced alignment
    #- 'asm5'      # asm-to-ref mapping, for ~0.1% sequence divergence
    #- 'asm10'     # asm-to-ref mapping, for ~1.0% sequence divergence
    #- 'asm20'     # asm-to-ref mapping, for ~5.0% sequence divergence
  path: 'resources/indexes/minimap2/'
  algorithm:
    k-mer_size: '21'     # -k: k-mer size (default: "21", no larger than "28") [INT]
    minimizer_size: '11' # -w: minimizer window size (default: "11") [INT]
    split_size: '8G'     # -I: split index for every {NUM} input bases (default: "8G") [INT]
    homopolymer:         # -H: use homopolymer-compressed k-mer  (default: no)
    # Available options, choose one: 
      - ''   # no (default)
      #- '-H' # yes (preferrable for PacBio)

###############################################################################
### BWA ###
###########
bwa:
  path: 'resources/indexes/bwa/' # Path to BWA indexes
  algorithm: # Algorithm for constructing BWA index (default: auto)
  # Available options, choose one:
    - ''         # Auto-select (default)
    #- '-a is'    # Moderately fast, but does not work with database larger than 2GB
    #- '-a bwtsw' # This method works with the whole human genome

###############################################################################
### BOWTIE2 ###
###############
bowtie2:
  sensitivity: # Bowtie2 'sensitivity' preset (default: "--very-sensitive")
  # Available preset options, choose one:
    - '--very-sensitive' # Same as options: "-D 20 -R 3 -N 0 -L 20 -i S,1,0.50" (default)
    #- '--sensitive'      # Same as options: "-D 15 -R 2 -N 0 -L 22 -i S,1,1.15"
    #- '--fast'           # Same as options: "-D 10 -R 2 -N 0 -L 22 -i S,0,2.50"    
    #- '--very-fast'      # Same as options: "-D 5 -R 1 -N 0 -L 22 -i S,0,2.50"
  path: 'resources/indexes/bowtie2/' # Path to Bowtie2 indexes (default path)
  algorithm: # Algorithm for constructing BT2 index
  # Available options, choose one:
    - ''              # (default)
    #- '--large-index' # Force generated index to be "large", even if reference has fewer than 4 billion nucleotides

################################################################################
### LOFREQ ###
##############
lofreq:
  min_af: '0.2' # Minimum allele frequency allowed (default: "0.2") [FLOAT]
  iupac:        # Output variants in the form of IUPAC ambiguity codes (default: deactivate)
  # Available options, choose one:
    - ''             # Deactivate (default)
    #- '--iupac-code' # Activate
  map_qual:     # Don't merge mapping quality in LoFreq's model (default: no='do not merge')
  # Available options, choose one:
    - '--no-mq' # no, don't merge mapping quality (default)
    #- ''        # yes, merge mapping quality (not recommanded with BWA or Bowtie2)

###############################################################################
### GISAID ### (soon)
##############
#gisaid:
#  username: 'username'
#  threshold: '30'
#  name: 'hCoV-19'
#  country: 'France'
#  identifier: ''
#  year: '2022'

###############################################################################
### ENVIRONNEMENTS ###
######################
conda:
  frontend: # Conda frontend (default: mamba)
  # Available options, choose one:
    - 'mamba' # mamba (faster)
    #- 'conda' # conda (iTrop)
  osx: # Conda OSX environement yaml files:
    bamclipper:    '../environments/osx/bamclipper_v.1.0.0.yaml'        # Bamclipper    ver. 1.0.0
    bcftools:      '../environments/osx/bcftools_v.1.17.yaml'           # Bcftools      ver. 1.17
    bedtools:      '../environments/osx/bedtools_v.2.31.0.yaml'         # Bedtools      ver. 2.31.0
    bowtie2:       '../environments/osx/bowtie2_v.2.5.1.yaml'           # Bowtie2       ver. 2.5.1
    bwa:           '../environments/osx/bwa_v.0.7.17.yaml'              # BWA           ver. 0.7.17
    cutadapt:      '../environments/osx/cutadapt_v.4.4.yaml'            # Cutadapt      ver. 4.4
    fastq_screen:  '../environments/osx/fastq-screen_v.0.15.3.yaml'     # Fastq-Screen  ver. 0.15.3
    fastqc:        '../environments/osx/fastqc_v.0.12.1.yaml'           # FastQC        ver. 0.12.1
    gawk:          '../environments/osx/gawk_v.5.1.0.yaml'              # Awk (GNU)     ver. 5.1.0
    lofreq:        '../environments/osx/lofreq_v.2.1.5.yaml'            # Lofreq        ver. 2.1.5
    multiqc:       '../environments/osx/multiqc_v.1.14.yaml'            # MultiQC       ver. 1.14
    minimap2:      '../environments/osx/minimap2_v.2.26.yaml'           # Minimap2      ver. 2.26
    nextclade:     '../environments/osx/nextclade_v.3.0.0.yaml'         # Nextclade     ver. 3.0.0
    pangolin:      '../environments/osx/pangolin_v.4.3.yaml'            # Pangolin      ver. 4.3
    samtools:      '../environments/osx/samtools_v.1.17.yaml'           # Samtools      ver. 1.17
    sickle_trim:   '../environments/osx/sickle-trim_v.1.33.yaml'        # Sickle-trim   ver. 1.33
    workflow_base: '../environments/osx/workflow-base_v.2024.01.yaml'   # Workflow-Base ver. 2024.01
  linux: # Conda Linux environement yaml files:
    bamclipper:    '../environments/linux/bamclipper_v.1.0.0.yaml'      # Bamclipper    ver. 1.0.0
    bcftools:      '../environments/linux/bcftools_v.1.17.yaml'         # Bcftools      ver. 1.17
    bedtools:      '../environments/linux/bedtools_v.2.31.0.yaml'       # Bedtools      ver. 2.31.0
    bowtie2:       '../environments/linux/bowtie2_v.2.5.1.yaml'         # Bowtie2       ver. 2.5.1
    bwa:           '../environments/linux/bwa_v.0.7.17.yaml'            # BWA           ver. 0.7.17
    cutadapt:      '../environments/linux/cutadapt_v.4.4.yaml'          # Cutadapt      ver. 4.4
    fastq_screen:  '../environments/linux/fastq-screen_v.0.15.3.yaml'   # Fastq-Screen  ver. 0.15.3
    fastqc:        '../environments/linux/fastqc_v.0.12.1.yaml'         # FastQC        ver. 0.12.1
    gawk:          '../environments/linux/gawk_v.5.1.0.yaml'            # Awk (GNU)     ver. 5.1.0
    lofreq:        '../environments/linux/lofreq_v.2.1.5.yaml'          # Lofreq        ver. 2.1.5
    minimap2:      '../environments/linux/minimap2_v.2.26.yaml'         # Minimap2      ver. 2.26
    multiqc:       '../environments/linux/multiqc_v.1.14.yaml'          # MultiQC       ver. 1.14
    nextclade:     '../environments/linux/nextclade_v.3.0.0.yaml'       # Nextclade     ver. 3.0.0
    pangolin:      '../environments/linux/pangolin_v.4.3.yaml'          # Pangolin      ver. 4.3
    samtools:      '../environments/linux/samtools_v.1.17.yaml'         # Samtools      ver. 1.17
    sickle_trim:   '../environments/linux/sickle-trim_v.1.33.yaml'      # Sickle-trim   ver. 1.33
    workflow_base: '../environments/linux/workflow-base_v.2024.01.yaml' # Workflow-Base ver. 2024.01

###############################################################################
### OPERATING SYSTEM ###
########################
os: 'osx' # Operating System (default: "osx", but will be set by Start_GeVarLi.sh)
# Available options, choose one::
  ###- 'osx'     # Mac OSX - Apple (R) [UNIX] (suported)
  ###- 'linux'   # GNU Linux - Free [UNIX] (suported)
  ###- 'bsd'     # Free BSD - Free [UNIX] (not tested. Need specific conda env. ?)
  ###- 'solaris' # Oracle Solaris - Sun Microsystems (R) [UNIX] (not tested. Need specific conda env. ?)
  ###- 'windows' # Windows - Microsoft Corporation (R) (not suported, not [UNIX])
